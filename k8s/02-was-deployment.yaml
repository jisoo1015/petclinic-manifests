# WAS Deployment (Spring Boot PetClinic)
# 이미지 URL은 ECR 푸시 후 수정 필요
apiVersion: apps/v1
kind: Deployment
metadata:
  name: was
  namespace: petclinic
  labels:
    app: petclinic
    tier: was
spec:
  replicas: 3
  selector:
    matchLabels:
      app: petclinic
      tier: was
  template:
    metadata:
      labels:
        app: petclinic
        tier: was
    spec:
      containers:
        - name: was
          # TODO: ECR URL로 변경 (terraform output ecr_was_url 참고)
          image: 723165663216.dkr.ecr.ap-northeast-2.amazonaws.com/petclinic-3tier-dev-was:build-10
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /petclinic/
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /petclinic/
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 30
            timeoutSeconds: 5
